Gradle

gradlew build -x test

Docker

docker build -t phonebook .
docker run -d phonebook

docker ps

docker-compose up -d
docker-compose down

docker-compose up --build

http://localhost:8080/swagger-ui.html
