apiVersion: v1
kind: Service
metadata:
  name: phonebook
  namespace: phonebook
spec:
  type: NodePort
  selector:
    app: phonebook
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
